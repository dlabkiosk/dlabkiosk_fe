{
  "name": "daesung-kiosk",
  "private": true,
  "workspaces": [
    "admin-web",
    "kiosk-app"
  ],
  "scripts": {
    "build:admin": "npm run build --workspace=admin-web",
    "build:kiosk": "npm run build --workspace=kiosk-app"
  }
}
