# 대성 키오스크 관리자 페이지 매뉴얼

> **최종 수정일:** 2026-04-14
> **적용 범위:** admin-web (관리자 웹 페이지)

---

## 목차

1. [로그인](#1-로그인)
2. [대시보드](#2-대시보드)
3. [출결 관리](#3-출결-관리)
4. [식사 신청 및 체크명단](#4-식사-신청-및-체크명단)
5. [휴대폰 미소지 관리](#5-휴대폰-미소지-관리)
6. [좌석 이탈 관리](#6-좌석-이탈-관리)
7. [좌석 관리](#7-좌석-관리)
8. [순공 관리](#8-순공-관리)
9. [공지 관리](#9-공지-관리)
10. [설정](#10-설정)
    - [10-1. 배너 관리](#10-1-배너-관리)
    - [10-2. 시험일정 관리](#10-2-시험일정-관리)
    - [10-3. 식단표 관리](#10-3-식단표-관리)
    - [10-4. 이탈사유 관리](#10-4-이탈사유-관리)
    - [10-5. 메시지 관리](#10-5-메시지-관리)
    - [10-6. 데이터 관리](#10-6-데이터-관리)
    - [10-7. 지점 정보](#10-7-지점-정보)
    - [10-8. 계정 관리 (ADMIN 전용)](#10-8-계정-관리-admin-전용)
    - [10-9. 내 정보](#10-9-내-정보)

---

## 권한 안내

관리자 페이지에는 두 가지 권한이 있습니다.

| 권한 | 설명 |
|------|------|
| **ADMIN** (총관리자) | 모든 지점을 관리할 수 있습니다. 모든 페이지에서 지점 필터 드롭다운이 표시됩니다. |
| **MANAGER** (지점 담당자) | 자신이 소속된 지점만 관리할 수 있습니다. 지점 필터가 표시되지 않습니다. |

> 이후 설명에서 **(ADMIN 전용)** 이라고 표기된 기능은 ADMIN 권한에서만 사용 가능합니다.

---

## 1. 로그인

**경로:** `/login`

> **참고:** 회원가입 기능은 제공되지 않습니다. 계정은 ADMIN이 설정 > 계정 관리(10-8)에서 등록합니다.

| 항목 | 설명 |
|------|------|
| 아이디 | 관리자 아이디를 입력합니다. |
| 비밀번호 | 관리자 비밀번호를 입력합니다. |
| 로그인 유지 | 체크 시 세션이 유지됩니다. |

**동작 흐름:**
1. 아이디와 비밀번호를 입력합니다.
2. `로그인` 버튼을 클릭합니다.
3. 인증 성공 시 대시보드(`/`)로 이동합니다.
4. 인증 실패 시 오류 메시지가 표시됩니다.

---

## 2. 대시보드

**경로:** `/`

대시보드는 오늘의 운영 현황을 한 눈에 보여주는 요약 페이지입니다.

### 지점 선택 (ADMIN 전용)
- 페이지 상단에 지점 선택 드롭다운이 표시됩니다.
- 지점을 선택하면 해당 지점의 데이터로 모든 카드가 갱신됩니다.

### 2-1. 일일 운영 현황 카드

현재 운영 중인 핵심 수치를 표시합니다.

| 항목 | 설명 |
|------|------|
| 등록 학생 | 해당 지점에 등록된 전체 학생 수 |
| 금일 등원 | 오늘 등원 처리된 학생 수 |
| 식사 신청 | 오늘 식사를 신청한 학생 수 |

### 2-2. 출결 현황 요약 카드

오늘의 출결 상태별 학생 수를 표시합니다.

| 상태 | 설명 |
|------|------|
| 출석 | 정상 등원한 학생 수 |
| 조퇴 | 조퇴 처리된 학생 수 |
| 결석 | 결석 처리된 학생 수 |
| 외출 | 외출 중인 학생 수 |
| 지각 | 지각 처리된 학생 수 |

- `더보기` 클릭 시 출결 관리 페이지(`/attendance`)로 이동합니다.

### 2-3. 좌석 이탈 현황 카드

| 항목 | 설명 |
|------|------|
| 금일 이탈 횟수 | 오늘 발생한 좌석 이탈 총 횟수 |
| 복귀 대기 | 현재 이탈 중이며 아직 복귀하지 않은 학생 수 |

- `더보기` 클릭 시 좌석 이탈 관리 페이지(`/seat-leaves`)로 이동합니다.

### 2-4. 식사 태그 현황 카드

최근 식사 태그(카드/QR 태그)된 학생 목록을 상위 5건까지 표시합니다.

| 열 | 설명 |
|----|------|
| 이름 | 학생 이름 |
| 식사 구분 | 중식 / 석식 |
| 태그 시간 | 식사 태그가 이루어진 시간 |

- 태그 내역이 없을 경우 "식사 태그 내역이 없습니다." 메시지가 표시됩니다.
- `더보기` 클릭 시 식사 관리 페이지(`/meals`)로 이동합니다.

### 2-5. 좌석 변경 신청현황 카드

학생이 키오스크에서 신청한 좌석 변경 요청을 상위 5건까지 표시합니다.

| 열 | 설명 |
|----|------|
| 이름 | 학생 이름 |
| 현재 → 희망 | 현재 좌석 → 희망 좌석 |
| 신청일 | 좌석 변경 신청 날짜 |

- 신청 내역이 없을 경우 "신청 내역이 없습니다." 메시지가 표시됩니다.
- `더보기` 클릭 시 좌석 관리 페이지(`/seats`)로 이동합니다.

### 2-6. 공지사항 카드

최근 등록된 공지사항을 상위 5건까지 표시합니다.

| 열 | 설명 |
|----|------|
| 번호 | 공지 번호 |
| 공지명 | 공지 제목 |
| 공지일 | 공지 등록일 |

- 공지사항이 없을 경우 "등록된 공지사항이 없습니다." 메시지가 표시됩니다.
- 공지 행을 클릭하면 해당 공지 상세 페이지로 이동합니다.
- `더보기` 클릭 시 공지 관리 페이지(`/notices`)로 이동합니다.

---

## 3. 출결 관리

**경로:** `/attendance`

학생들의 출결 현황을 조회하고, QR 코드를 다운로드하며, CSV로 내보낼 수 있는 페이지입니다.

### 3-1. 필터

| 필터 | 설명 |
|------|------|
| 지점 **(ADMIN 전용)** | 조회할 지점을 선택합니다. |
| 학생 이름 | 학생 이름으로 검색합니다. |
| 학번 | 학번으로 검색합니다. |
| 출결 상태 | 전체 / 등원 / 미출석 / 외출 / 하원 / 공석 / 미확인 / 좌석이탈 중 선택합니다. |
| 핸드폰 제출 | 전체 / O / X 중 선택합니다. |

### 3-2. 출결 목록 테이블

| 열 | 설명 |
|----|------|
| 체크박스 | 행 선택 (일괄 동작에 사용) |
| 이름 | 학생 이름 (정렬 가능) |
| 학번 | 학생 학번 (정렬 가능) |
| 좌석 | 배정된 좌석 번호 (정렬 가능) |
| 출결 상태 | 색상 배지로 표시 (정렬 가능) |
| 핸드폰 제출 | O 또는 X (정렬 가능) |

**출결 상태 배지 색상:**
| 상태 | 색상 |
|------|------|
| 등원 | 초록색 |
| 미출석 | 빨간색 |
| 외출 | 주황색 |
| 하원 | 회색 |
| 좌석이탈 | 빨간색 |
| 공석 | 회색 |
| 미확인 | 회색 |

### 3-3. 행 클릭 → QR 코드 모달

테이블의 행을 클릭하면 해당 학생의 QR 코드 모달이 열립니다.

**모달 기능:**
- QR 코드 이미지 표시
- `다운로드` 버튼: QR 코드를 PNG 이미지로 저장합니다.
- `프린트` 버튼: QR 코드를 인쇄합니다.

### 3-4. 일괄 동작

| 버튼 | 설명 |
|------|------|
| QR 다운로드 | 체크박스로 선택한 학생들의 QR 코드를 ZIP 파일로 일괄 다운로드합니다. |
| CSV 내보내기 | 현재 필터링된 또는 선택된 학생 데이터를 CSV 파일로 내보냅니다. |

### 3-5. 페이지네이션

- 한 페이지에 15건씩 표시됩니다.
- 하단의 페이지 번호 또는 `‹` `›` 버튼으로 페이지를 이동합니다.

---

## 4. 식사 신청 및 체크명단

**경로:** `/meals`

학생들의 식사 신청 상태와 태그(체크) 여부를 관리하는 페이지입니다.

### 4-1. 필터

| 필터 | 설명 |
|------|------|
| 지점 **(ADMIN 전용)** | 조회할 지점을 선택합니다. |
| 학생 이름 | 학생 이름으로 검색합니다. |
| 학번 | 학번으로 검색합니다. |
| 날짜 | 조회할 날짜를 선택합니다. (기본값: 오늘) |

### 4-2. 식사 목록 테이블

| 열 | 설명 |
|----|------|
| 체크박스 | 행 선택 |
| 이름 | 학생 이름 (정렬 가능) |
| 학번 | 학번 (정렬 가능) |
| 좌석 | 좌석 번호 (정렬 가능) |
| 중식 신청 | 중식 신청 여부 |
| 중식 체크시간 | 중식 태그 시간 (정렬 가능) |
| 석식 신청 | 석식 신청 여부 |
| 석식 체크시간 | 석식 태그 시간 (정렬 가능) |

### 4-3. 식사 상태 표시

| 표시 | 의미 | 색상 |
|------|------|------|
| O | 신청 완료 & 태그 완료 | 기본 |
| 미신청 | 식사를 신청하지 않음 | 회색 |
| 미체크 | 신청했으나 아직 태그하지 않음 | 빨간색 (클릭 가능) |
| 확인 필요 | 미신청이나 식사 시간에 태그가 감지됨 | 노란색 |

### 4-4. 수동 식사 체크

- `미체크` 상태의 셀을 클릭하면 확인 모달이 표시됩니다.
- 확인을 누르면 해당 학생의 식사가 체크(태그) 처리됩니다.
- 이미 체크된 항목은 다시 클릭해도 변경되지 않습니다.

### 4-5. CSV 내보내기

- `CSV 내보내기` 버튼을 클릭하면 선택한 날짜의 식사 데이터를 CSV 파일로 다운로드합니다.

### 4-6. 페이지네이션

- 한 페이지에 15건씩 표시됩니다.

---

## 5. 휴대폰 미소지 관리

**경로:** `/phones`

학생의 휴대폰 미소지 신고 내역을 관리하는 페이지입니다.

### 5-1. 필터

| 필터 | 설명 |
|------|------|
| 지점 **(ADMIN 전용)** | 조회할 지점을 선택합니다. |
| 학생 이름 | 학생 이름으로 검색합니다. |
| 학번 | 학번으로 검색합니다. |
| 날짜 | 조회할 기준 날짜를 선택합니다. 선택한 날짜가 신고 기간(시작일~종료일)에 포함되는 항목만 표시됩니다. |

### 5-2. 미소지 목록 테이블

| 열 | 설명 |
|----|------|
| 체크박스 | 행 선택 (일괄 삭제에 사용) |
| 이름 | 학생 이름 (정렬 가능) |
| 학번 | 학번 (정렬 가능) |
| 좌석 | 좌석 번호 (정렬 가능) |
| 미소지 유형 | 신고 유형 배지 (아래 참조) |
| 보호자 연락처 | 보호자 전화번호 (정렬 가능) |
| 메모 | 메모 내용 (20자 초과 시 말줄임 표시, 정렬 가능) |

### 5-3. 미소지 유형 배지

| 유형 | 표시 형식 |
|------|-----------|
| 휴대폰 없음 | "휴대폰 없음" |
| 기간 지정 (퇴원까지) | "YYYY-MM-DD ~ 퇴원까지" |
| 기간 지정 (종료일 있음) | "YYYY-MM-DD ~ YYYY-MM-DD" |

### 5-4. 행 클릭 → 상세 모달

테이블의 행을 클릭하면 상세 정보 모달이 열립니다.

**모달에 표시되는 정보:**
- 학생 이름, 학번, 좌석
- 미소지 유형
- 보호자 연락처
- 메모 (전체 내용)

### 5-5. 수정 모달

상세 모달에서 `수정` 버튼을 클릭하면 수정 모달이 열립니다.

**수정 가능 항목:**
| 항목 | 설명 |
|------|------|
| 보호자 연락처 | 보호자 전화번호를 수정합니다. |
| 메모 | 메모 내용을 수정합니다. |

### 5-6. 삭제

- **개별 삭제:** 상세 모달에서 `삭제` 버튼을 클릭합니다.
- **일괄 삭제:** 체크박스로 여러 항목을 선택한 후 `선택 삭제` 버튼을 클릭합니다.
- 삭제 시 확인 모달이 표시됩니다.

### 5-7. CSV 내보내기

- 현재 필터링된 또는 선택된 데이터를 CSV 파일로 다운로드합니다.

---

## 6. 좌석 이탈 관리

**경로:** `/seat-leaves`

학생들의 좌석 이탈 기록을 관리하고, 강제 복귀 처리를 할 수 있는 페이지입니다.

### 6-1. 필터

| 필터 | 설명 |
|------|------|
| 지점 **(ADMIN 전용)** | 조회할 지점을 선택합니다. |
| 학생 이름 | 학생 이름으로 검색합니다. |
| 학번 | 학번으로 검색합니다. |
| 시작일 | 조회 기간의 시작일을 선택합니다. |
| 종료일 | 조회 기간의 종료일을 선택합니다. |

### 6-2. 이탈 목록 테이블

| 열 | 설명 |
|----|------|
| 체크박스 | 행 선택 (일괄 강제 복귀에 사용) |
| 이름 | 학생 이름 |
| 학번 | 학번 |
| 좌석 | 좌석 번호 |
| 이탈 요청 일시 | 이탈 신청한 날짜와 시간 |
| 상태 | 이탈중 (빨간색) / 복귀 (회색) |
| 경과 시간 | 이탈 시작부터 현재(또는 복귀)까지 경과 시간 (예: "1h 30m", "45m") |
| 이탈 사유 | 학생이 선택한 이탈 사유 |

### 6-3. 경과 시간 실시간 갱신

- "이탈중" 상태의 학생에 대해 경과 시간이 **60초마다 자동 갱신**됩니다.
- 복귀 완료된 학생의 경과 시간은 최종 값으로 고정됩니다.

### 6-4. 강제 복귀

**개별 강제 복귀:**
- "이탈중" 상태인 행에만 `강제 복귀` 버튼이 표시됩니다.
- 버튼 클릭 시 확인 모달이 표시되고, 확인하면 해당 학생이 강제로 복귀 처리됩니다.

**일괄 강제 복귀:**
1. 체크박스로 여러 학생을 선택합니다.
2. `선택 강제 복귀` 버튼을 클릭합니다.
3. 선택된 학생 중 "이탈중" 상태인 학생만 복귀 처리됩니다. (이미 복귀한 학생은 무시됩니다.)
4. 버튼에 선택된 이탈중 학생 수가 표시됩니다.

### 6-5. CSV 내보내기

- 경과 시간, 이탈 사유 등을 포함한 데이터를 CSV 파일로 다운로드합니다.

---

## 7. 좌석 관리

**경로:** `/seats`

좌석 배치, 좌석 정보 관리, 좌석 변경 요청 처리를 수행하는 페이지입니다.

### 7-1. 보기 모드 전환

페이지 상단에 두 가지 보기 모드를 전환할 수 있습니다.

| 모드 | 설명 |
|------|------|
| 목록 보기 | 좌석 정보를 테이블 형태로 표시합니다. |
| 배치도 보기 | 좌석을 캔버스 기반 시각적 레이아웃으로 표시합니다. |

### 7-2. 배치도 보기 (캔버스 뷰)

좌석을 시각적으로 배치한 화면입니다.

**좌석 현황 요약:**
| 항목 | 설명 |
|------|------|
| 등록 좌석 | 전체 등록된 좌석 수 |
| 점유 | 현재 사용 중인 좌석 수 |
| 공석 | 비어 있는 좌석 수 |

**좌석 상태 색상:**
| 상태 | 색상 | 의미 |
|------|------|------|
| 학습중 | 초록색 | 현재 학습 중인 학생이 앉아 있음 |
| 외출 | 주황색 | 좌석 주인이 외출 중 |
| 하원 | 회색 | 좌석 주인이 하원함 |
| 좌석이탈 | 빨간색 | 좌석에서 이탈 중 |

**좌석 검색:**
- 좌석 검색 입력란에 좌석명을 입력하면 해당 좌석이 하이라이트됩니다.

**우클릭 컨텍스트 메뉴 (보기 모드):**
| 메뉴 | 동작 |
|------|------|
| 상세 | 좌석 상세 정보 모달을 엽니다. |
| 수정 | 좌석 정보 수정 모달을 엽니다. |
| 삭제 | 좌석을 삭제합니다 (확인 후). |

### 7-3. 배치 수정 모드

`배치 수정` 버튼을 클릭하면 배치 편집 모드로 진입합니다.

**편집 모드에서 가능한 동작:**
- **좌석 드래그:** 좌석을 드래그하여 위치를 변경합니다.
- **좌석 추가:** `+ 좌석 추가` 버튼으로 새 좌석을 생성합니다.

**저장/취소:**
- `저장` 버튼: 변경된 좌석 배치를 저장합니다.
- `취소` 버튼: 변경 사항을 무시하고 편집 모드를 종료합니다.

### 7-4. 좌석 추가/수정 모달

| 항목 | 설명 |
|------|------|
| 구역명 | 좌석이 속한 구역 (드롭다운 선택) |
| 좌석명 | 좌석 이름 (예: A-01, B-12) |
| 좌석 타입 | 개인(INDIVIDUAL) / 그룹(GROUPED) |
| X 좌표 | 배치도에서의 X 좌표값 |
| Y 좌표 | 배치도에서의 Y 좌표값 |

### 7-5. 좌석 상세 모달

좌석을 클릭하면 해당 좌석의 상세 정보가 표시됩니다.

**표시 정보:**
- 좌석명, 좌석 타입, 구역
- 현재 배정된 학생 정보
- 좌석 상태

### 7-6. 대기 목록 모달

좌석을 클릭하면 해당 좌석의 대기자 목록을 확인할 수 있습니다.

| 열 | 설명 |
|----|------|
| 학생명 | 대기 중인 학생 이름 |
| 현재 좌석 | 학생의 현재 좌석 |
| 1순위 | 1순위 희망 좌석 |
| 2순위 | 2순위 희망 좌석 |
| 3순위 | 3순위 희망 좌석 |
| 신청일 | 좌석 변경 신청 날짜 |

### 7-7. 좌석 변경 요청 처리

학생이 키오스크에서 신청한 좌석 변경 요청을 관리합니다.

**요청 상태:**
| 상태 | 설명 |
|------|------|
| 대기중 | 아직 처리되지 않은 요청 |
| 승인 | 관리자가 승인한 요청 |
| 거절 | 관리자가 거절한 요청 |

**승인 흐름:**
1. 대기중인 요청을 클릭합니다.
2. 학생이 희망한 좌석 목록(최대 3순위)이 표시됩니다.
3. 희망 좌석 중 하나를 선택하여 `승인` 버튼을 클릭합니다.
4. 해당 학생의 좌석이 선택된 좌석으로 변경됩니다.

**거절 흐름:**
1. 대기중인 요청에서 `거절` 버튼을 클릭합니다.
2. 확인 후 요청이 거절 처리됩니다.

---

## 8. 순공 관리

**경로:** `/study-time`

학생들의 순공시간(순수 공부 시간)을 기간별로 조회하는 페이지입니다.

### 8-1. 필터

| 필터 | 설명 |
|------|------|
| 지점 **(ADMIN 전용)** | 조회할 지점을 선택합니다. |
| 학생 이름 | 학생 이름으로 검색합니다. |
| 학번 | 학번으로 검색합니다. |
| 기간 시작일 | 조회 기간의 시작일 (기본값: 7일 전) |
| 기간 종료일 | 조회 기간의 종료일 (기본값: 오늘) |

### 8-2. 순공시간 테이블

| 열 | 설명 |
|----|------|
| 이름 | 학생 이름 (정렬 가능) |
| 학번 | 학번 (정렬 가능) |
| 좌석 | 좌석 번호 (정렬 가능) |
| 합계 | 조회 기간 내 총 순공시간 (정렬 가능) |
| 날짜별 열 | 기간 내 각 날짜의 순공시간 (MM/DD(요일) 형식) |

**테이블 특징:**
- 처음 5개 열(이름, 학번, 좌석, 합계, 첫 날짜)은 **고정 열**로, 가로 스크롤 시에도 항상 표시됩니다.
- 날짜별 열은 선택한 기간에 따라 동적으로 생성됩니다.
- 기간이 길 경우 가로 스크롤이 필요합니다.

### 8-3. CSV 내보내기

- 선택한 기간의 순공시간 데이터를 CSV 파일로 다운로드합니다.
- 모든 날짜별 데이터가 포함됩니다.

### 8-4. 페이지네이션

- 한 페이지에 15건씩 표시됩니다.

---

## 9. 공지 관리

### 9-1. 공지 목록

**경로:** `/notices`

등록된 공지사항을 조회하고 관리하는 페이지입니다.

#### 필터

| 필터 | 설명 |
|------|------|
| 지점 **(ADMIN 전용)** | 조회할 지점을 선택합니다. |
| 말머리 | 카테고리별 필터링 (전체 / 사용자 지정 말머리) |
| 공지 제목 | 제목으로 검색합니다. |

#### 공지 목록 테이블

| 열 | 설명 |
|----|------|
| 체크박스 | 행 선택 (일괄 삭제에 사용) |
| 번호 | 공지 번호 |
| 제목 | 공지 제목 ([말머리] 형식으로 표시) |
| 날짜 | 공지 등록일 |
| 지점 | 공지가 등록된 지점 |

**고정 공지 (📌):**
- 고정 공지는 목록 최상단에 📌 아이콘과 함께 표시됩니다.
- 고정 공지끼리는 등록일 순으로 정렬됩니다.

#### 동작

| 버튼 | 설명 |
|------|------|
| 공지 작성 | 공지 작성 페이지(`/notices/new`)로 이동합니다. |
| 선택 삭제 | 체크박스로 선택한 공지를 일괄 삭제합니다. (확인 후) |
| 설정 (⚙) | 공지 설정 모달(말머리 관리)을 엽니다. |

#### 행 클릭

- 공지 행을 클릭하면 공지 상세 페이지(`/notices/:noticeId`)로 이동합니다.

### 9-2. 공지 설정 모달 (말머리 관리)

설정(⚙) 버튼을 클릭하면 열리는 모달입니다.

**모달 제목:** 공지 설정

**기능:**
- 현재 등록된 말머리 목록이 표시됩니다.
- **드래그하여 순서 변경:** 말머리 항목을 드래그하여 표시 순서를 변경할 수 있습니다.
- **말머리 추가:** 하단 입력란에 새 말머리를 입력하고 `+ 추가` 버튼을 클릭합니다.
- **말머리 삭제:** 각 항목 오른쪽의 `X` 버튼을 클릭합니다.
- `저장` 버튼을 눌러 변경 사항을 반영합니다.
- `취소` 버튼을 누르면 변경 사항 없이 모달을 닫습니다.

**기본 말머리:**
- 전체, 국어, 수학, 과학, 사회, 한국사

> **주의:** 추가 후 반드시 `저장` 버튼을 눌러야 적용됩니다.

### 9-3. 공지 작성

**경로:** `/notices/new`

| 항목 | 설명 |
|------|------|
| 지점 **(ADMIN 전용)** | 공지를 등록할 지점을 선택합니다. |
| 말머리 | 드롭다운에서 말머리를 선택합니다. (공지 설정에서 관리한 목록) |
| 제목 | 공지 제목을 입력합니다. (최대 100자, 글자 수 카운터 표시) |
| 내용 | 공지 본문을 입력합니다. (최대 1000자, 글자 수 카운터 표시) |
| 고정 공지 | 토글 버튼으로 고정 여부를 설정합니다. 활성화하면 공지 목록 최상단에 고정됩니다. |

**미리보기:**
- 입력 중 실시간으로 `[말머리] 제목` 형식의 미리보기가 표시됩니다.

**유효성 검사:**
- 모든 항목은 필수입니다.
- 말머리가 "선택" 상태이면 등록할 수 없습니다.

**버튼:**
| 버튼 | 동작 |
|------|------|
| 등록 | 공지를 저장하고 공지 목록으로 이동합니다. |
| 취소 | 작성을 취소하고 공지 목록으로 돌아갑니다. |

### 9-4. 공지 상세/수정

**경로:** `/notices/:noticeId`

#### 보기 모드

**표시 정보:**
- 제목: [말머리] 제목 형식
- 메타 정보: 지점명, 고정 배지, 작성자, 작성일시
- 본문 내용

**동작:**
- 우측 상단 케밥 메뉴(⋮)를 클릭하면 다음 옵션이 표시됩니다:

| 메뉴 | 동작 |
|------|------|
| 수정 | 수정 모드로 전환합니다. |
| 삭제 | 확인 후 공지를 삭제하고 목록으로 돌아갑니다. |

- `뒤로가기` 버튼으로 공지 목록으로 돌아갑니다.

#### 수정 모드

공지 작성과 동일한 폼이 표시되며, 추가로 다음 항목이 있습니다:

| 항목 | 설명 |
|------|------|
| 활성/비활성 토글 | 공지의 노출 여부를 설정합니다. |
| 지점 변경 **(ADMIN 전용)** | 공지의 소속 지점을 변경할 수 있습니다. |

**버튼:**
| 버튼 | 동작 |
|------|------|
| 저장 | 수정 내용을 저장합니다. |
| 취소 | 수정을 취소하고 보기 모드로 돌아갑니다. |

---

## 10. 설정

**경로:** `/settings`

설정 페이지는 탭 기반 인터페이스로, 9개의 관리 탭으로 구성됩니다.

> **참고:** '계정 관리' 탭은 ADMIN 권한에서만 표시됩니다. MANAGER 권한에서는 8개 탭이 표시됩니다.

---

### 10-1. 배너 관리

키오스크 화면에 표시되는 배너(이미지/영상)를 관리합니다.

#### 지점 선택 (ADMIN 전용)
- 드롭다운에서 지점을 선택합니다. (placeholder: "지점을 선택해주세요")

#### 배너 등록/수정

| 항목 | 설명 |
|------|------|
| 파일 | 이미지 또는 영상 파일을 업로드합니다. 클릭하여 파일을 선택하거나 드래그 앤 드롭합니다. |
| 미디어 타입 | 이미지 / 영상 중 선택합니다. |
| 노출 시간 (초) | 키오스크에서 해당 배너가 표시되는 시간(초)을 설정합니다. |
| 활성 상태 | 체크박스로 활성/비활성을 설정합니다. |

**파일 업로드 안내:**
- 권장 비율: 16:9 (예: 1920×1080)
- 업로드 후 미리보기가 표시되며, **드래그하여 잘림 위치를 조정**할 수 있습니다.
- 수정 시 `파일 변경` 버튼으로 파일을 교체할 수 있습니다.

#### 배너 목록

| 열 | 설명 |
|----|------|
| 순서 | 배너 표시 순서 (드래그로 변경 가능) |
| 미리보기 | 배너 썸네일 |
| 미디어 타입 | 이미지 / 영상 |
| 노출 시간 | 설정된 노출 시간(초) |
| 상태 | 노출중 (활성) / 비활성 |
| 관리 | 수정 / 삭제 버튼 |

**배너 순서 변경:**
- 배너 목록에서 행을 **드래그하여 순서를 변경**할 수 있습니다.
- 변경된 순서는 키오스크에 즉시 반영됩니다.

**버튼:**
| 버튼 | 동작 |
|------|------|
| 새로 등록 | 신규 배너 등록 폼을 엽니다. |
| 저장 | 배너를 등록/수정합니다. |
| 수정 | 기존 배너를 수정 모드로 전환합니다. |
| 취소 | 등록/수정을 취소합니다. |

- 배너가 없을 경우 "등록된 배너가 없습니다." 메시지가 표시됩니다.

---

### 10-2. 시험일정 관리

키오스크에 표시되는 시험일정을 관리합니다.

#### 보기 모드 전환

| 모드 | 설명 |
|------|------|
| 목록 보기 | 시험일정을 테이블로 표시합니다. |
| 캘린더 보기 | 시험일정을 월별 캘린더로 표시합니다. |

#### 목록 보기

**검색:**
- 검색란에 시험명을 입력하여 필터링합니다. (placeholder: "시험명을 검색하세요.")

**테이블 열:**

| 열 | 설명 |
|----|------|
| 체크박스 | 행 선택 (일괄 삭제에 사용) |
| 시험명 | 시험 이름 |
| 날짜 | 시험 날짜 (정렬 가능: ▲ ▼) |
| 지점 | 소속 지점 |
| 상태 | 예정 / 진행중 / 종료 |
| 키오스크 | ON/OFF 토글 (지점별 최대 2개) |
| 관리 | 수정 / 삭제 버튼 |

**시험 상태:**
| 상태 | 의미 |
|------|------|
| 예정 | 아직 시작되지 않은 시험 |
| 진행중 | 현재 진행 중인 시험 |
| 종료 | 이미 종료된 시험 |

#### 캘린더 보기

- 월별 캘린더에 시험일정이 표시됩니다.
- 날짜를 클릭하면 해당 날짜의 시험 목록이 표시됩니다.
- 캘린더에서 직접 수정/삭제가 가능합니다.

#### 시험 추가/수정 모달

| 항목 | 설명 |
|------|------|
| 지점 | 시험이 표시될 지점을 선택합니다. **(ADMIN 전용 - 추가 시)** |
| 시험명 | 시험 이름을 입력합니다. (최대 15자, 글자 수 카운터 표시) |
| 날짜 | 시험 날짜를 선택합니다. |

**버튼:**
| 버튼 | 동작 |
|------|------|
| 등록 | 신규 시험을 등록합니다. |
| 수정 | 기존 시험 정보를 수정합니다. |
| 삭제 | 시험을 삭제합니다. (빨간색 버튼) |
| + 추가 일정 생성 | 같은 모달에서 추가 시험을 바로 등록합니다. |

**일괄 삭제:**
- 체크박스로 여러 시험을 선택한 후 `선택 삭제` 버튼을 클릭합니다.

---

### 10-3. 식단표 관리

키오스크에 표시되는 주간 식단표를 관리합니다.

#### 지점 선택 (ADMIN 전용)
- 드롭다운에서 지점을 선택합니다.

#### 식단표 표시

두 장의 카드로 이번주와 다음주 식단표가 표시됩니다.

**카드 구성:**
- **이번주 식단표** 카드
- **다음주 식단표** 카드

각 카드에는 주간 식단이 테이블로 표시됩니다:

| 열 | 설명 |
|----|------|
| 요일 | 월 ~ 일 |
| 중식 | 중식 메뉴 |
| 석식 | 석식 메뉴 |

**특수 표시:**
- 오늘 날짜에는 **TODAY** 배지가 표시됩니다.
- 메뉴가 없는 경우 `-` (대시)로 표시됩니다.
- 휴무일은 "휴무"로 표시됩니다.

#### 식단표 등록/수정 모달

`식단표 등록` 버튼을 클릭하면 모달이 열립니다.

**모달 제목:** {주차} 식단표 등록 / {주차} 식단표 수정
- 예: "이번주 식단표 등록", "다음주 식단표 수정"

**모달 상단:** 해당 주의 날짜 범위 표시 (MM/DD~MM/DD)

**입력 테이블:**

| 열 | 설명 |
|----|------|
| 날짜 | 요일과 날짜 |
| 중식 | 중식 메뉴 입력 (textarea, placeholder: "중식 메뉴 입력") |
| 석식 | 석식 메뉴 입력 (textarea, placeholder: "석식 메뉴 입력") |
| 휴무 | 체크박스 (체크 시 해당 요일 식단 입력 비활성화) |

**버튼:**
| 버튼 | 동작 |
|------|------|
| 저장 | 식단표를 저장합니다. |
| 취소 | 모달을 닫습니다. |

---

### 10-4. 이탈사유 관리

키오스크에서 학생이 좌석 이탈 시 선택할 수 있는 사유를 관리합니다.

#### 안내 문구
- "※ 최대 {최대개수}개까지 등록할 수 있습니다."

#### 사유 목록

| 열 | 설명 |
|----|------|
| 드래그 핸들 | 드래그하여 순서를 변경합니다. |
| 순서 | 표시 순서 번호 |
| 아이콘 | 사유 아이콘 이미지 |
| 사유명 | 사유 이름 |
| 관리 | 수정 / 삭제 버튼 |

- 사유가 없을 경우 "등록된 사유가 없습니다." 메시지가 표시됩니다.

#### 사유 추가/수정 모달

| 항목 | 설명 |
|------|------|
| 지점 | 소속 지점 선택 **(ADMIN 전용 - 추가 시)** |
| 사유명 | 사유 이름을 입력합니다. (최대 10자, 글자 수 카운터 표시, placeholder: "예: 화장실, 상담 등") |
| 아이콘 | 사유 아이콘 이미지를 업로드합니다. |

**아이콘 업로드:**
- `이미지 선택` 버튼으로 파일을 선택합니다.
- `이미지 제거` 버튼으로 업로드된 이미지를 제거합니다.
- 업로드 후 미리보기가 표시됩니다.
- 안내: "정사각형 이미지 권장 (PNG/JPG/SVG, 최대 5MB)"
- 수정 시: "새 이미지를 선택하지 않으면 기존 아이콘이 유지됩니다."

**버튼:**
| 버튼 | 동작 |
|------|------|
| 등록 | 신규 사유를 등록합니다. |
| 수정 | 기존 사유를 수정합니다. |
| 삭제 | 사유를 삭제합니다. (빨간색 버튼) |

---

### 10-5. 메시지 관리

메시지 관리 탭은 두 개의 섹션으로 구성됩니다.

#### A. 메시지 템플릿 관리

자주 사용하는 메시지를 템플릿으로 등록하고 관리합니다.

**지점 필터 (ADMIN 전용):**
- 드롭다운에서 지점을 선택합니다. (전체 / 개별 지점)

**템플릿 목록 테이블:**

| 열 | 설명 |
|----|------|
| 지점 | 템플릿이 등록된 지점 |
| 내용 | 템플릿 메시지 내용 |
| 등록일 | 템플릿 등록 날짜 |
| 관리 | 수정 / 삭제 / 학생 관리 버튼 |

- 템플릿이 10개를 초과하면 페이지네이션이 표시됩니다.
- 템플릿이 없을 경우 "등록된 템플릿이 없습니다." 메시지가 표시됩니다.

**템플릿 추가/수정 모달:**

| 항목 | 설명 |
|------|------|
| 지점 | 소속 지점 선택 **(ADMIN 전용 - 추가 시)** (placeholder: "지점 선택") |
| 내용 | 템플릿 메시지 내용 (최대 50자, 글자 수 카운터 표시, placeholder: "메시지 템플릿 내용을 입력하세요") |

**버튼:**
| 버튼 | 동작 |
|------|------|
| + 템플릿 추가 | 신규 템플릿 등록 모달을 엽니다. |
| 등록 | 템플릿을 저장합니다. |
| 수정 | 템플릿을 수정합니다. |

##### 템플릿 등록 학생 관리

템플릿 목록에서 학생 관리 버튼을 클릭하면 해당 템플릿에 등록된 학생 목록을 관리할 수 있습니다.

**모달 제목:** [{지점명}] - [{템플릿 내용}]에 등록된 학생 ({N}명)

**학생 검색:**
- 검색란에 이름 또는 학번을 입력합니다. (placeholder: "이름 또는 학번 검색")

**학생 목록 테이블:**

| 열 | 설명 |
|----|------|
| 체크박스 | 학생 선택 (일괄 삭제에 사용) |
| 학생명 | 학생 이름 |
| 학번 | 학번 |
| 삭제 | 개별 삭제 버튼 |

**동작:**
| 버튼 | 동작 |
|------|------|
| + 학생 추가 | 템플릿에 학생을 추가합니다. |
| 선택 삭제 | 체크박스로 선택한 학생을 일괄 삭제합니다. (빨간색 버튼) |

#### B. 학생 개인 메시지 관리

개별 학생에게 커스텀 메시지 또는 템플릿 메시지를 할당합니다.

**지점 필터 (ADMIN 전용):**
- 드롭다운에서 지점을 선택합니다.

**화면 구성: 좌우 분할**

**좌측 패널 — 학생 선택:**
- 검색란에 학생 이름 또는 학번을 입력합니다. (placeholder: "학생 이름 또는 학번 검색")
- 학생 목록에서 학생을 클릭하여 선택합니다.
- 각 학생은 "{이름} {학번}" 형식으로 표시됩니다.
- 학생이 선택되지 않은 경우 "왼쪽에서 학생을 선택하세요." 메시지가 표시됩니다.

**우측 패널 — 메시지 목록:**
- 선택된 학생의 헤더: "{학생이름} ({학번})"

**메시지 테이블:**

| 열 | 설명 |
|----|------|
| 구분 | 메시지 유형 배지 |
| 내용 | 메시지 내용 |
| 등록일 | 메시지 등록 날짜 |
| 관리 | 수정 / 삭제 버튼 |

**메시지 유형 배지:**
| 배지 | 색상 | 의미 |
|------|------|------|
| 템플릿 | 파란색 | 템플릿에서 할당된 메시지 |
| 커스텀 | 초록색 | 개별적으로 작성된 메시지 |

**메시지 추가 모달:**

| 항목 | 설명 |
|------|------|
| 지점 | 소속 지점 **(ADMIN 전용)** |
| 템플릿 선택 | 기존 템플릿 중 하나를 선택합니다. (placeholder: "-- 템플릿을 선택하세요 --") |
| 학생 선택 | 메시지를 할당할 학생을 선택합니다. (검색 가능, 복수 선택 가능) |

> **참고:** 커스텀 메시지를 직접 입력하는 중에는 템플릿 선택이 비활성화됩니다. ("커스텀 메시지 입력 중에는 선택할 수 없습니다" 안내 표시)

**메시지 삭제:**
- 커스텀 메시지만 개별 삭제가 가능합니다. (빨간색 삭제 버튼)
- 템플릿 메시지는 템플릿 학생 관리에서 제거해야 합니다.

---

### 10-6. 데이터 관리

외부 DSA(대성아카데미) 서버로부터 지점 및 학생 데이터를 동기화합니다.

**섹션 제목:** DSA 데이터 동기화

**설명:** "DSA 서버에서 최신 데이터를 가져와 동기화합니다."

#### 지점 동기화

**설명:** "DSA에 등록된 지점(학원) 목록을 동기화합니다. 새 지점은 자동 생성되고, 기존 지점은 이름이 업데이트됩니다."

**동작 흐름:**
1. `지점 동기화` 버튼을 클릭합니다.
2. 버튼이 "동기화 중..."으로 변경되며 비활성화됩니다.
3. 동기화 완료 후 결과 테이블이 표시됩니다.

**결과 테이블:**

| 열 | 설명 |
|----|------|
| DSA 전체 | DSA에서 가져온 전체 지점 수 |
| 신규 | 새로 생성된 지점 수 |
| 업데이트 | 기존에서 정보가 갱신된 지점 수 |
| 변경없음 | 변경이 없는 지점 수 |

#### 학생 동기화

**설명:** "DSA에 등록된 학생 정보를 동기화합니다. 전체 지점의 학생 데이터를 가져옵니다."

**동작 흐름:**
1. `학생 동기화` 버튼을 클릭합니다.
2. 버튼이 "동기화 중..."으로 변경되며 비활성화됩니다.
3. 동기화 완료 후 지점별 결과 테이블이 표시됩니다.

**결과 테이블 (지점별 행):**

| 열 | 설명 |
|----|------|
| 지점명 | 동기화된 지점 이름 |
| DSA 전체 | DSA에서 가져온 해당 지점의 전체 학생 수 |
| 신규 | 새로 생성된 학생 수 |
| 업데이트 | 기존에서 정보가 갱신된 학생 수 |
| 변경없음 | 변경이 없는 학생 수 |
| 실패 | 동기화에 실패한 학생 수 |

**오류 표시:**
- 동기화 중 오류가 발생한 경우 오류 메시지 목록이 표시됩니다.

---

### 10-7. 지점 정보

> **참고:** 지점 추가/삭제 기능은 제공되지 않습니다. 지점은 DSA 데이터 동기화(10-6)를 통해 생성되며, 이 탭에서는 **수정만** 가능합니다.

#### ADMIN 화면 — 전체 지점 목록

ADMIN 권한에서는 모든 지점을 목록으로 관리합니다.

**지점 목록 테이블:**

| 열 | 설명 |
|----|------|
| 지점코드 | 지점 고유 코드 (예: DS-001) |
| 지점명 | 지점 이름 |
| 주소 | 지점 주소 |
| 전화번호 | 지점 전화번호 |
| 상태 | 활성 / 비활성 배지 |
| DSA | 연결 / 미연결 배지 |

- 지점이 없을 경우 "등록된 지점이 없습니다." 메시지가 표시됩니다.
- 지점 행을 클릭하면 **지점 상세 페이지**(`/settings/branch/:storeId`)로 이동합니다.

#### MANAGER 화면 — 단일 지점 상세

MANAGER 권한에서는 자신의 소속 지점 정보만 인라인으로 표시됩니다.

#### 지점 상세 페이지

**경로:** `/settings/branch/:storeId`

**페이지 구성:**
- 상단: `← 뒤로가기` 버튼으로 설정 페이지로 돌아갑니다.
- 제목: "지점 정보"

**보기 모드에서 표시되는 정보:**

| 항목 | 설명 |
|------|------|
| 지점명 | 지점 이름 |
| 지점코드 | 지점 고유 코드 (읽기 전용) |
| 주소 | 지점 주소 |
| 전화번호 | 지점 전화번호 |
| 활성화 | 활성 / 비활성 배지 |
| DSA 연결 | 연결됨 / 미연결 배지 |
| 키오스크 PIN | 키오스크 인증용 4자리 PIN |
| DSA 학원코드 | DSA 연동용 학원 코드 |
| DSA Client ID | DSA API Client ID |
| DSA Secret ID | DSA API Secret ID |

**수정 모드:**
- `수정` 버튼을 클릭하면 수정 모드로 전환됩니다.
- 각 필드가 입력 가능한 상태로 변경됩니다.
- 지점코드는 수정 불가 (읽기 전용).
- PIN 변경 시: placeholder "변경 시 입력" 표시, 유효성 검사 "PIN은 숫자 4자리로 입력해주세요."
- DSA 관련 항목: placeholder "변경 시 입력"
- 수정 모드 진입 시 DSA 필드 위에 안내 문구가 표시됩니다:
  > "DSA 학원코드, Client ID, Secret ID는 DSA에서 발급받아 정확히 입력해야 키오스크 및 관리자 페이지가 정상 작동합니다."

**버튼:**
| 버튼 | 동작 |
|------|------|
| 수정 | 수정 모드로 전환합니다. |
| 저장 | 변경 사항을 저장합니다. (수정 모드) |
| 취소 | 수정을 취소하고 보기 모드로 돌아갑니다. (수정 모드) |

**오류/로딩 상태:**
- 로딩 중: "로딩 중..." 표시
- 지점을 찾을 수 없을 경우: "지점 정보를 찾을 수 없습니다." 표시
- 데이터 로드 실패 시: "지점 정보를 불러올 수 없습니다." 표시

---

### 10-8. 계정 관리 (ADMIN 전용)

관리자 계정을 등록/수정/삭제하는 탭입니다. **ADMIN 권한에서만 표시됩니다.**

#### 검색

- 검색란에 로그인 ID 또는 이름을 입력하여 계정을 검색합니다. (placeholder: "로그인 ID 또는 이름 검색")

#### 계정 목록 테이블

| 열 | 설명 |
|----|------|
| 로그인 ID | 관리자 로그인 ID |
| 이름 | 관리자 이름 |
| 역할 | ADMIN / MANAGER 배지 |
| 소속 지점 | 소속 지점명 (ADMIN은 표시 없음) |
| 관리 | 수정(연필 아이콘) / 삭제(휴지통 아이콘) 버튼 |

- 계정이 없거나 검색 결과가 없을 경우 안내 메시지가 표시됩니다.

#### 페이지네이션

- 15개 계정마다 페이지가 나뉩니다.

#### 계정 등록 모달

`+ 계정 등록` 버튼을 클릭하면 모달이 열립니다.

| 항목 | 필수 | 설명 |
|------|------|------|
| 로그인 ID | ✅ | 사용할 로그인 ID (중복 검사: 기존 ID와 중복 시 "이미 사용 중인 로그인 ID입니다." 오류 표시) |
| 비밀번호 | ✅ | 비밀번호 (눈 아이콘으로 표시/숨김 전환, 8자 미만 시 "비밀번호는 8자 이상이어야 합니다." 오류 표시) |
| 이름 | ✅ | 관리자 이름 |
| 역할 | ✅ | ADMIN / MANAGER 드롭다운 선택 |
| 소속 지점 | 조건부 | 역할이 MANAGER일 때만 표시되며 필수. 역할이 ADMIN이면 숨김 처리됨 |

**유효성 검사:**
- 모든 필수 항목이 입력되지 않으면 각 항목 아래에 오류 메시지가 표시됩니다.
- 로그인 ID 중복 시 등록이 차단됩니다.
- 비밀번호 8자 미만 시 등록이 차단됩니다.

#### 계정 수정 모달

목록에서 수정(연필) 아이콘을 클릭하면 모달이 열립니다.

| 항목 | 설명 |
|------|------|
| 로그인 ID | 읽기 전용 (수정 불가) |
| 비밀번호 | 선택 사항 (입력하면 변경, 비우면 기존 비밀번호 유지. 눈 아이콘으로 표시/숨김 전환) |
| 이름 | 수정 가능 |
| 역할 | ADMIN / MANAGER 드롭다운 변경 가능 |
| 소속 지점 | 역할이 MANAGER일 때만 표시 |

#### 계정 삭제

- 목록에서 삭제(휴지통) 아이콘을 클릭하면 확인 모달이 표시됩니다.
- 확인 후 계정이 삭제됩니다.

---

### 10-9. 내 정보

현재 로그인된 관리자의 프로필을 확인하고 수정하는 탭입니다. **ADMIN과 MANAGER 모두 접근 가능합니다.**

#### 표시 정보

| 항목 | 설명 |
|------|------|
| 로그인 ID | 읽기 전용 |
| 이름 | 수정 가능 |
| 역할 | ADMIN / MANAGER 배지 (읽기 전용) |
| 소속 지점 | 소속 지점명 (읽기 전용, **ADMIN일 경우 표시되지 않음**) |

#### 수정 모드

`수정` 버튼을 클릭하면 수정 모드로 전환됩니다.

**수정 가능 항목:**
| 항목 | 설명 |
|------|------|
| 이름 | 이름을 변경합니다. |
| 비밀번호 | 새 비밀번호를 입력합니다. (눈 아이콘으로 표시/숨김 전환. 비우면 기존 비밀번호 유지. 8자 미만 시 오류 표시) |

**이름 변경 시:**
- 저장 후 상단 네비게이션 바의 관리자 이름이 실시간으로 갱신됩니다.

**버튼:**
| 버튼 | 동작 |
|------|------|
| 수정 | 수정 모드로 전환합니다. (보기 모드) |
| 저장 | 변경 사항을 저장합니다. (수정 모드) |
| 취소 | 수정을 취소하고 보기 모드로 돌아갑니다. (수정 모드) |

---

## 공통 UI 패턴

### 확인 모달

삭제, 강제 복귀 등 중요한 동작을 수행할 때 확인 모달이 표시됩니다.

**일반적인 확인 메시지:**
- "정말 삭제하시겠습니까?"
- "이 지점을 삭제하시겠습니까?"
- "해당 주 식단을 삭제하시겠습니까?"
- 등

### 정렬

- 테이블 열 헤더를 클릭하면 해당 열 기준으로 정렬됩니다.
- 클릭할 때마다 오름차순(▲) ↔ 내림차순(▼) 전환됩니다.

### 페이지네이션

- 대부분의 목록 페이지에서 한 페이지당 15건씩 표시됩니다.
- 하단에 페이지 번호와 `‹` `›` 이동 버튼이 표시됩니다.

### 필수 입력 표시

- 필수 항목에는 `*` (별표)가 표시됩니다.

### 글자 수 카운터

- 글자 수 제한이 있는 입력란에는 `{현재}/{최대}` 형식의 카운터가 표시됩니다.

### 로딩 상태

- 데이터를 불러오는 중일 때 "로딩 중..." 메시지가 표시됩니다.
- 저장/등록 중일 때 버튼이 "저장 중...", "등록 중...", "동기화 중..." 등으로 변경되며 비활성화됩니다.

### 빈 상태

- 데이터가 없을 때 해당 영역에 안내 메시지가 표시됩니다.
  - 예: "등록된 배너가 없습니다.", "검색 결과가 없습니다."

---

## 사이드바 메뉴 구성

| 순서 | 메뉴명 | 경로 | 설명 |
|------|--------|------|------|
| 1 | 대시보드 | `/` | 운영 현황 요약 |
| 2 | 출결 관리 | `/attendance` | 학생 출결 관리 |
| 3 | 식사 신청 및 체크명단 | `/meals` | 식사 신청/체크 관리 |
| 4 | 휴대폰 미소지 관리 | `/phones` | 휴대폰 미소지 신고 관리 |
| 5 | 좌석 이탈 관리 | `/seat-leaves` | 좌석 이탈 기록/강제 복귀 |
| 6 | 좌석 관리 | `/seats` | 좌석 배치/변경 요청 관리 |
| 7 | 순공 관리 | `/study-time` | 순공시간 조회 |
| 8 | 공지 관리 | `/notices` | 공지사항 관리 |
| 9 | 설정 | `/settings` | 시스템 설정 (9개 탭, MANAGER는 8개) |

**사이드바 접기/펼치기:**
- 사이드바 상단의 토글 버튼을 클릭하면 사이드바가 아이콘만 표시되는 축소 모드로 전환됩니다.
- 접기 상태는 로컬 스토리지에 저장되어 새로고침 후에도 유지됩니다.
